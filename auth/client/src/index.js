import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

//Renders the App.js file to the DOM
ReactDOM.render(
  <App />,
  document.querySelector('#root')
);
